# calculate the area of rectangle

length = float(input('Enter the length: '))
breadth = float(input('Enter the breadth: '))

# Perform the Operation
area = length*breadth

# Diaplay the Output
print('Area of Reactnagle:', area)

